<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hhwork.dao.ProductDao">
	
	<select id="select" parameterType="java.util.Map" resultType="com.hhwork.model.Product">
		select 
		a.id,
		a.name,
		a.descr,
		a.mainPhoto,
		a.status,
		a.templateId,
		a.createTime,
		a.createMan 
		from product 
		<where>
			<if test="id!=null"> AND a.id = #{id}</if>
			<if test="name!=null"> AND a.name = #{name}</if>
		</where>
	</select>
	
	<select id="selectPresention" parameterType="java.util.Map" resultType="com.hhwork.model.Presentation">
		select 
		p.id,
		p.productId,
		p.num,
		p.html,
		p.createTime,
		p.createMan,
		pro.name proName 
		from presentation p 
		left join product pro on p.productId = pro.id 
		<where>
			<if test="productId!=null"> AND p.productId = #{productId}</if>
		</where>
	</select>
	
</mapper>